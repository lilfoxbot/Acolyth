- tallboi turrets (occupy 2 voxels)
- only spawn turrets on floor

- tar source
- tar spread

- spawn tile

- player collision that dont suck
- save/reset level layouts
- level loader

=== BUGS ===

- player clips through voxels if falling too fast
- player clips through top level voxels

=== NOTES ===

▼ inspiration
- tempest (1981)
- plants vs zombies
- creeper world
- rocket slime

▼ ideas
- win condition: tar source destroyed
- fail condition: tar reaches player spawn
- small enclosed voxel environments
- encroaching tar voxels
- player can shoot and place turrets to fight tar
- creeper kills player on touch, returns player to spawn
- sky view of level (spawner view)
- blastoff transition between levels (tempest)

▼ NEW ENVIRONMENT SETUP

1 - install raylib

2 - add to ENV PATH variable:
C:\raylib\w64devkit\bin

3 - Fix build path in tasks.json:
tasks.json > tasks > windows > command -> "C:/raylib/w64devkit/bin/mingw32-make.exe" instead of "mingw32-make.exe"